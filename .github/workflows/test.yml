name: Action Tests

on:
    push:
        branches:
            - "main"
        paths-ignore:
            - "README.md"
            - "LICENSE"
            - ".github/*.md"
            - "docs/**"
    pull_request:
        branches:
            - "main"

permissions:
    contents: read
    security-events: write
    pull-requests: write

env:
    ANSIBLE_HOST_KEY_CHECKING: "false"
    ANSIBLE_DEPRECATION_WARNINGS: "false"
    ANSIBLE_FORCE_COLOR: "true"
    PY_COLORS: "1"
    GOPROXY: "https://proxy.golang.org,direct"

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    setup-environment:
        name: Setup Environment
        runs-on: ubuntu-latest
        timeout-minutes: 5
        outputs:
            cache-key: ${{ steps.cache-key.outputs.key }}
            test-level: ${{ steps.test-config.outputs.level }}
            go-version: ${{ steps.versions.outputs.go }}
            ansible-version: ${{ steps.versions.outputs.ansible }}
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Determine versions
              id: versions
              run: |
                  GO_VERSION=$(grep '^go ' go.mod | cut -d' ' -f2)
                  ANSIBLE_VERSION=$(grep 'FROM arillso/ansible:' Dockerfile | cut -d':' -f2 | cut -d'@' -f1)
                  echo "go=${GO_VERSION}" >> "$GITHUB_OUTPUT"
                  echo "ansible=${ANSIBLE_VERSION}" >> "$GITHUB_OUTPUT"

            - name: Configure test level
              id: test-config
              env:
                  GH_EVENT_NAME: ${{ github.event_name }}
              run: |
                  if [[ "${GH_EVENT_NAME}" == "pull_request" ]]; then
                      echo "level=basic" >> "$GITHUB_OUTPUT"
                  else
                      echo "level=full" >> "$GITHUB_OUTPUT"
                  fi

            - name: Generate cache key
              id: cache-key
              env:
                  HASH_FILES: ${{ hashFiles('**/go.sum', 'Dockerfile', 'tests/**/*', 'action.yml') }}
              run: |
                  echo "key=action-tests-${HASH_FILES}" >> "$GITHUB_OUTPUT"

    build-action:
        name: Build Action
        needs: setup-environment
        runs-on: ubuntu-latest
        timeout-minutes: 10
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Setup Go
              uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6
              with:
                  go-version: ${{ needs.setup-environment.outputs.go-version }}
                  cache: true

            - name: Build Go binary
              run: |
                  go build -ldflags="-s -w" -o action-playbook .

            - name: Cache built binary
              uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5
              with:
                  path: action-playbook
                  key: ${{ needs.setup-environment.outputs.cache-key }}-binary

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Build Docker image
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  push: false
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test binary execution
              run: |
                  ./action-playbook --help

    test-basic:
        name: Basic Function Test
        needs: [setup-environment, build-action]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Run basic playbook
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  execution_timeout: 10
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-advanced:
        name: Advanced Playbook Test
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Run advanced playbook
              uses: ./
              with:
                  playbook: tests/advanced_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  execution_timeout: 15
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-modes:
        name: Test Execution Modes
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        strategy:
            matrix:
                mode: [check, diff, syntax_check, verbose]
            fail-fast: false
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Run with ${{ matrix.mode }} mode
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  check: ${{ matrix.mode == 'check' }}
                  diff: ${{ matrix.mode == 'diff' }}
                  syntax_check: ${{ matrix.mode == 'syntax_check' }}
                  verbose: ${{ matrix.mode == 'verbose' && '3' || '' }}
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-parameters:
        name: Test Parameters
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 20
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test with tags
              uses: ./
              with:
                  playbook: tests/tagged_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  tags: "tag1"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test with skip-tags
              uses: ./
              with:
                  playbook: tests/tagged_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  skip_tags: "tag2"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test with extra-vars
              uses: ./
              with:
                  playbook: tests/extra_vars_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  extra_vars: "my_var=test_value env_var=from_extra_vars"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test with limit hosts
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  limit: "localhost"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-ssh-key-handling:
        name: SSH Key Format Tests
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Generate test SSH key (Unix LF format)
              run: |
                  ssh-keygen -t rsa -b 2048 -f /tmp/test_key_lf -N "" -C "test@example.com"
                  {
                    echo "TEST_KEY_LF<<EOF"
                    cat /tmp/test_key_lf
                    echo ""
                    echo "EOF"
                  } >> "$GITHUB_ENV"

            - name: Generate test SSH key (Windows CRLF format)
              run: |
                  ssh-keygen -t rsa -b 2048 -f /tmp/test_key_crlf -N "" -C "test@example.com"
                  # Convert LF to CRLF (Windows line endings)
                  sed -i 's/$/\r/' /tmp/test_key_crlf
                  {
                    echo "TEST_KEY_CRLF<<EOF"
                    cat /tmp/test_key_crlf
                    echo ""
                    echo "EOF"
                  } >> "$GITHUB_ENV"

            - name: Generate test SSH key (No trailing newline)
              run: |
                  ssh-keygen -t rsa -b 2048 -f /tmp/test_key_no_newline -N "" -C "test@example.com"
                  # Remove trailing newline
                  truncate -s -1 /tmp/test_key_no_newline
                  {
                    echo "TEST_KEY_NO_NEWLINE<<EOF"
                    cat /tmp/test_key_no_newline
                    echo ""
                    echo "EOF"
                  } >> "$GITHUB_ENV"

            - name: Test with LF formatted key
              uses: ./
              with:
                  playbook: tests/ssh_key_test_playbook.yml
                  inventory: tests/hosts.yml
                  private_key: ${{ env.TEST_KEY_LF }}
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"

            - name: Test with CRLF formatted key (should auto-normalize)
              uses: ./
              with:
                  playbook: tests/ssh_key_test_playbook.yml
                  inventory: tests/hosts.yml
                  private_key: ${{ env.TEST_KEY_CRLF }}
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"

            - name: Test with key missing trailing newline (should auto-fix)
              uses: ./
              with:
                  playbook: tests/ssh_key_test_playbook.yml
                  inventory: tests/hosts.yml
                  private_key: ${{ env.TEST_KEY_NO_NEWLINE }}
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"

            # kics-scan ignore-block
            - name: Test with invalid key format (should fail with validation)
              uses: ./
              with:
                  playbook: tests/ssh_key_test_playbook.yml
                  inventory: tests/hosts.yml
                  private_key: "INVALID_TEST_KEY_FORMAT"  # Test value, not a real secret
              continue-on-error: true
              id: test-invalid-key

            - name: Verify invalid key was rejected
              env:
                  INVALID_KEY_OUTCOME: ${{ steps.test-invalid-key.outcome }}
              run: |
                  if [[ "${INVALID_KEY_OUTCOME}" == "success" ]]; then
                      echo "Expected failure for invalid SSH key but got success"
                      exit 1
                  fi
                  echo "Invalid key was correctly rejected"

    test-error-handling:
        name: Error Handling Tests
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test with non-existent playbook
              uses: ./
              with:
                  playbook: tests/non_existent.yml
                  inventory: tests/hosts.yml
              continue-on-error: true
              id: test-missing-playbook

            - name: Test with invalid inventory
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/invalid_hosts.yml
              continue-on-error: true
              id: test-invalid-inventory

            - name: Verify error handling
              env:
                  MISSING_PLAYBOOK_OUTCOME: ${{ steps.test-missing-playbook.outcome }}
                  INVALID_INVENTORY_OUTCOME: ${{ steps.test-invalid-inventory.outcome }}
              run: |
                  if [[ "${MISSING_PLAYBOOK_OUTCOME}" == "success" ]]; then
                      echo "Expected failure for missing playbook but got success"
                      exit 1
                  fi
                  if [[ "${INVALID_INVENTORY_OUTCOME}" == "success" ]]; then
                      echo "Expected failure for invalid inventory but got success"
                      exit 1
                  fi
                  echo "Error handling tests passed"

    test-listing:
        name: Test Listing Modes
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        strategy:
            matrix:
                mode: [list_hosts, list_tags, list_tasks]
            fail-fast: false
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Run with ${{ matrix.mode }}
              uses: ./
              with:
                  playbook: tests/tagged_playbook.yml
                  inventory: tests/hosts.yml
                  list_hosts: ${{ matrix.mode == 'list_hosts' }}
                  list_tags: ${{ matrix.mode == 'list_tags' }}
                  list_tasks: ${{ matrix.mode == 'list_tasks' }}
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-execution-options:
        name: Test Execution Options
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 20
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test dry_run mode (check + diff combined)
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  dry_run: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test flush_cache
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  flush_cache: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test force_handlers (playbook has a failing task)
              uses: ./
              continue-on-error: true
              id: test-force-handlers
              with:
                  playbook: tests/handler_failure_playbook.yml
                  inventory: tests/hosts.yml
                  force_handlers: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Verify force_handlers was processed
              run: |
                  echo "force_handlers test completed (playbook fails but handlers should run)"

            - name: Test custom forks
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  forks: "10"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test start_at_task
              uses: ./
              with:
                  playbook: tests/start_at_task_playbook.yml
                  inventory: tests/hosts.yml
                  start_at_task: "Target task - start here"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-multi-input:
        name: Test Multiple Inputs
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 20
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test multiple playbooks (comma-separated)
              uses: ./
              with:
                  playbook: "tests/basic_playbook.yml,tests/second_playbook.yml"
                  inventory: tests/hosts.yml
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test multiple playbooks (multiline)
              uses: ./
              with:
                  playbook: |
                      tests/basic_playbook.yml
                      tests/second_playbook.yml
                  inventory: tests/hosts.yml
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test multiple inventories (comma-separated)
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: "tests/hosts.yml,tests/second_inventory.yml"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test multiple inventories (multiline)
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: |
                      tests/hosts.yml
                      tests/second_inventory.yml
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test multiline extra_vars
              uses: ./
              with:
                  playbook: tests/extra_vars_playbook.yml
                  inventory: tests/hosts.yml
                  extra_vars: |
                      my_var=multiline_value
                      env_var=from_multiline
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-vault:
        name: Test Vault
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Generate vault test password
              id: vault-creds
              run: |
                  VAULT_PASS=$(openssl rand -hex 16)
                  echo "vault_pass=${VAULT_PASS}" >> "$GITHUB_OUTPUT"

            - name: Create vault-encrypted vars file
              env:
                  VAULT_PASSWORD: ${{ steps.vault-creds.outputs.vault_pass }}
              run: |
                  pip install ansible-core==2.17.*
                  echo "vault_secret: decrypted_successfully" > /tmp/vault_vars.yml
                  ansible-vault encrypt /tmp/vault_vars.yml --vault-password-file <(printf '%s' "${VAULT_PASSWORD}")
                  cp /tmp/vault_vars.yml tests/vault_vars.yml

            - name: Test with vault_password
              uses: ./
              with:
                  playbook: tests/vault_playbook.yml
                  inventory: tests/hosts.yml
                  vault_password: ${{ steps.vault-creds.outputs.vault_pass }}
                  extra_vars: "@tests/vault_vars.yml"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-output:
        name: Test Output Options
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test output_file
              uses: ./
              id: test-output-file
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  output_file: ansible_output.txt
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Verify output file was created
              run: |
                  if [[ ! -f ansible_output.txt ]]; then
                      echo "Output file was not created"
                      exit 1
                  fi
                  echo "Output file contents:"
                  cat ansible_output.txt
                  if [[ ! -s ansible_output.txt ]]; then
                      echo "Output file is empty"
                      exit 1
                  fi
                  echo "Output file test passed"

            - name: Verify action outputs on success
              uses: ./
              id: test-outputs-success
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Check success outputs
              env:
                  SUCCESS_STATUS: ${{ steps.test-outputs-success.outputs.status }}
                  SUCCESS_EXIT_CODE: ${{ steps.test-outputs-success.outputs.exit_code }}
              run: |
                  echo "Status: ${SUCCESS_STATUS}"
                  echo "Exit code: ${SUCCESS_EXIT_CODE}"
                  if [[ "${SUCCESS_STATUS}" != "success" ]]; then
                      echo "Expected status 'success' but got '${SUCCESS_STATUS}'"
                      exit 1
                  fi
                  if [[ "${SUCCESS_EXIT_CODE}" != "0" ]]; then
                      echo "Expected exit_code '0' but got '${SUCCESS_EXIT_CODE}'"
                      exit 1
                  fi
                  echo "Output verification passed"

            - name: Verify action outputs on failure
              uses: ./
              id: test-outputs-failure
              continue-on-error: true
              with:
                  playbook: tests/failing_playbook.yml
                  inventory: tests/hosts.yml
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Check failure outputs
              env:
                  FAILURE_STATUS: ${{ steps.test-outputs-failure.outputs.status }}
                  FAILURE_EXIT_CODE: ${{ steps.test-outputs-failure.outputs.exit_code }}
              run: |
                  echo "Status: ${FAILURE_STATUS}"
                  echo "Exit code: ${FAILURE_EXIT_CODE}"
                  if [[ "${FAILURE_STATUS}" != "failed" ]]; then
                      echo "Expected status 'failed' but got '${FAILURE_STATUS}'"
                      exit 1
                  fi
                  if [[ "${FAILURE_EXIT_CODE}" == "0" ]]; then
                      echo "Expected non-zero exit_code but got '0'"
                      exit 1
                  fi
                  echo "Failure output verification passed"

    test-lint:
        name: Test Lint
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test with lint enabled (expect failure - ansible-lint not in image)
              uses: ./
              continue-on-error: true
              id: test-lint-run
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  lint: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Verify lint flag was processed
              env:
                  LINT_OUTCOME: ${{ steps.test-lint-run.outcome }}
              run: |
                  echo "Lint step outcome: ${LINT_OUTCOME}"
                  if [[ "${LINT_OUTCOME}" == "success" ]]; then
                      echo "ansible-lint is available and passed"
                  elif [[ "${LINT_OUTCOME}" == "failure" ]]; then
                      echo "Lint step failed - acceptable if ansible-lint is not installed"
                  else
                      echo "Unexpected outcome: ${LINT_OUTCOME}"
                      exit 1
                  fi

    test-retries:
        name: Test Retries
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test retries with failing playbook
              uses: ./
              continue-on-error: true
              id: test-retries
              with:
                  playbook: tests/failing_playbook.yml
                  inventory: tests/hosts.yml
                  retries: "2"
                  retry_delay: "1"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Verify retries were attempted and action still failed
              env:
                  RETRIES_OUTCOME: ${{ steps.test-retries.outcome }}
              run: |
                  if [[ "${RETRIES_OUTCOME}" == "success" ]]; then
                      echo "Expected failure after retries but got success"
                      exit 1
                  fi
                  echo "Retries test passed - action failed as expected after retries"

            - name: Test retries with succeeding playbook
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  retries: "1"
                  retry_delay: "1"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-privilege-escalation:
        name: Test Privilege Escalation
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test with become flags (syntax check - sudo not in container)
              uses: ./
              with:
                  playbook: tests/become_playbook.yml
                  inventory: tests/hosts.yml
                  become: "true"
                  become_method: "sudo"
                  become_user: "root"
                  syntax_check: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-galaxy-options:
        name: Test Galaxy Options
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 20
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test galaxy_force (reinstall)
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  galaxy_force: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test galaxy_no_deps
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  galaxy_no_deps: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test galaxy_timeout
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  galaxy_timeout: "120"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test galaxy_force_with_deps
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  galaxy_force_with_deps: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test galaxy_ignore_certs
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  galaxy_file: tests/requirements.yml
                  galaxy_ignore_certs: "true"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-connection:
        name: Test Connection Options
        needs: [setup-environment, test-basic]
        runs-on: ubuntu-latest
        timeout-minutes: 15
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

            - name: Pre-build with cache
              uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7
              with:
                  context: .
                  load: true
                  tags: action-playbook:test
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

            - name: Update action.yml for testing
              run: |
                  sed -i 's|image: ".*"|image: "docker://action-playbook:test"|' action.yml

            - name: Test with explicit connection type
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  connection: "local"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Fetch GitHub known_hosts
              id: known-hosts
              run: |
                  {
                    echo "value<<KNOWN_HOSTS_EOF"
                    ssh-keyscan github.com 2>/dev/null
                    echo "KNOWN_HOSTS_EOF"
                  } >> "$GITHUB_OUTPUT"

            - name: Test with known_hosts
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  connection: "local"
                  known_hosts: ${{ steps.known-hosts.outputs.value }}
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

            - name: Test with timeout
              uses: ./
              with:
                  playbook: tests/basic_playbook.yml
                  inventory: tests/hosts.yml
                  connection: "local"
                  timeout: "60"
              env:
                  ANSIBLE_HOST_KEY_CHECKING: "false"
                  ANSIBLE_DEPRECATION_WARNINGS: "false"

    test-summary:
        name: Test Summary
        needs:
            - setup-environment
            - test-basic
            - test-advanced
            - test-modes
            - test-parameters
            - test-ssh-key-handling
            - test-error-handling
            - test-listing
            - test-execution-options
            - test-multi-input
            - test-vault
            - test-output
            - test-lint
            - test-retries
            - test-privilege-escalation
            - test-galaxy-options
            - test-connection
        runs-on: ubuntu-latest
        permissions:
            pull-requests: write
            contents: read
        if: always()
        steps:
            - name: Checkout code
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

            - name: Generate comprehensive test summary
              env:
                  RESULT_BASIC: ${{ needs.test-basic.result }}
                  RESULT_ADVANCED: ${{ needs.test-advanced.result }}
                  RESULT_MODES: ${{ needs.test-modes.result }}
                  RESULT_PARAMETERS: ${{ needs.test-parameters.result }}
                  RESULT_SSH_KEY: ${{ needs.test-ssh-key-handling.result }}
                  RESULT_ERROR: ${{ needs.test-error-handling.result }}
                  RESULT_LISTING: ${{ needs.test-listing.result }}
                  RESULT_EXEC_OPTIONS: ${{ needs.test-execution-options.result }}
                  RESULT_MULTI_INPUT: ${{ needs.test-multi-input.result }}
                  RESULT_VAULT: ${{ needs.test-vault.result }}
                  RESULT_OUTPUT: ${{ needs.test-output.result }}
                  RESULT_LINT: ${{ needs.test-lint.result }}
                  RESULT_RETRIES: ${{ needs.test-retries.result }}
                  RESULT_PRIVILEGE: ${{ needs.test-privilege-escalation.result }}
                  RESULT_GALAXY: ${{ needs.test-galaxy-options.result }}
                  RESULT_CONNECTION: ${{ needs.test-connection.result }}
              run: |
                  ./tests/test-summary.sh "Action-Tests" \
                  "Basic Tests=${RESULT_BASIC}" \
                  "Advanced Tests=${RESULT_ADVANCED}" \
                  "Mode Tests=${RESULT_MODES}" \
                  "Parameter Tests=${RESULT_PARAMETERS}" \
                  "SSH Key Handling Tests=${RESULT_SSH_KEY}" \
                  "Error Handling Tests=${RESULT_ERROR}" \
                  "Listing Mode Tests=${RESULT_LISTING}" \
                  "Execution Options Tests=${RESULT_EXEC_OPTIONS}" \
                  "Multi-Input Tests=${RESULT_MULTI_INPUT}" \
                  "Vault Tests=${RESULT_VAULT}" \
                  "Output Tests=${RESULT_OUTPUT}" \
                  "Lint Tests=${RESULT_LINT}" \
                  "Retry Tests=${RESULT_RETRIES}" \
                  "Privilege Escalation Tests=${RESULT_PRIVILEGE}" \
                  "Galaxy Options Tests=${RESULT_GALAXY}" \
                  "Connection Tests=${RESULT_CONNECTION}"

            - name: Upload test artifacts
              if: always()
              uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
              with:
                  name: action-test-results-${{ github.run_id }}
                  path: |
                      ./Action-Tests-results.md
                      ./**/test-*.log
                  retention-days: 30

            - name: Comment on pull request with test results
              if: github.event_name == 'pull_request'
              uses: thollander/actions-comment-pull-request@24bffb9b452ba05a4f3f77933840a6a841d1b32b # v3
              with:
                  file-path: ./Action-Tests-results.md
                  comment-tag: action-test-results
                  mode: upsert

            - name: Test quality gate
              env:
                  RESULT_BASIC: ${{ needs.test-basic.result }}
                  RESULT_ADVANCED: ${{ needs.test-advanced.result }}
                  RESULT_MODES: ${{ needs.test-modes.result }}
                  RESULT_PARAMETERS: ${{ needs.test-parameters.result }}
                  RESULT_SSH_KEY: ${{ needs.test-ssh-key-handling.result }}
                  RESULT_LISTING: ${{ needs.test-listing.result }}
                  RESULT_EXEC_OPTIONS: ${{ needs.test-execution-options.result }}
                  RESULT_MULTI_INPUT: ${{ needs.test-multi-input.result }}
                  RESULT_VAULT: ${{ needs.test-vault.result }}
                  RESULT_OUTPUT: ${{ needs.test-output.result }}
                  RESULT_LINT: ${{ needs.test-lint.result }}
                  RESULT_RETRIES: ${{ needs.test-retries.result }}
                  RESULT_PRIVILEGE: ${{ needs.test-privilege-escalation.result }}
                  RESULT_GALAXY: ${{ needs.test-galaxy-options.result }}
                  RESULT_CONNECTION: ${{ needs.test-connection.result }}
              run: |
                  failed_jobs=()
                  if [[ "${RESULT_BASIC}" == "failure" ]]; then
                      failed_jobs+=("Basic Tests")
                  fi
                  if [[ "${RESULT_ADVANCED}" == "failure" ]]; then
                      failed_jobs+=("Advanced Tests")
                  fi
                  if [[ "${RESULT_MODES}" == "failure" ]]; then
                      failed_jobs+=("Mode Tests")
                  fi
                  if [[ "${RESULT_PARAMETERS}" == "failure" ]]; then
                      failed_jobs+=("Parameter Tests")
                  fi
                  if [[ "${RESULT_SSH_KEY}" == "failure" ]]; then
                      failed_jobs+=("SSH Key Handling Tests")
                  fi
                  if [[ "${RESULT_LISTING}" == "failure" ]]; then
                      failed_jobs+=("Listing Mode Tests")
                  fi
                  if [[ "${RESULT_EXEC_OPTIONS}" == "failure" ]]; then
                      failed_jobs+=("Execution Options Tests")
                  fi
                  if [[ "${RESULT_MULTI_INPUT}" == "failure" ]]; then
                      failed_jobs+=("Multi-Input Tests")
                  fi
                  if [[ "${RESULT_VAULT}" == "failure" ]]; then
                      failed_jobs+=("Vault Tests")
                  fi
                  if [[ "${RESULT_OUTPUT}" == "failure" ]]; then
                      failed_jobs+=("Output Tests")
                  fi
                  if [[ "${RESULT_LINT}" == "failure" ]]; then
                      failed_jobs+=("Lint Tests")
                  fi
                  if [[ "${RESULT_RETRIES}" == "failure" ]]; then
                      failed_jobs+=("Retry Tests")
                  fi
                  if [[ "${RESULT_PRIVILEGE}" == "failure" ]]; then
                      failed_jobs+=("Privilege Escalation Tests")
                  fi
                  if [[ "${RESULT_GALAXY}" == "failure" ]]; then
                      failed_jobs+=("Galaxy Options Tests")
                  fi
                  if [[ "${RESULT_CONNECTION}" == "failure" ]]; then
                      failed_jobs+=("Connection Tests")
                  fi

                  if [[ ${#failed_jobs[@]} -gt 0 ]]; then
                      echo "Critical tests failed: ${failed_jobs[*]}"
                      exit 1
                  fi

                  echo "All critical tests passed"
