---
- name: Extra Vars Playbook
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Print extra variable
      ansible.builtin.debug:
        msg: "The greeting is: {{ greeting | default('no greeting') }}"
